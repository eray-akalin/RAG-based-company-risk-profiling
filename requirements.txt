# Pinned to a verified-working set. The sentence-transformers 5.x -> datasets ->
# pyarrow import chain breaks on version drift, so these are exact pins.

# Core
requests==2.32.5
beautifulsoup4==4.14.2
lxml==6.1.0

# Text Processing
# NOTE: langchain pinned <0.4 — langchain 1.x breaks ragas 0.2.x imports.
langchain==0.3.30
langchain-text-splitters==0.3.11
tiktoken==0.12.0

# Embeddings & Vector Store
sentence-transformers==5.4.1
faiss-cpu==1.13.2
rank-bm25==0.2.2

# LLM
transformers==5.8.0
torch==2.11.0
accelerate==1.13.0

# Transitive deps of the sentence-transformers import chain — pinned because
# this is exactly where unpinned resolution fails on deploy.
datasets==4.8.5
pyarrow==24.0.0
huggingface-hub==1.14.0
tokenizers==0.22.2
safetensors==0.7.0

# Dashboard
streamlit==1.57.0
plotly==6.7.0

# Evaluation
scikit-learn==1.7.2
pandas==2.3.2
numpy==2.3.2
# RAGAS (LLM-judged metrics). Judge = Groq llama-3.3-70b via langchain-groq;
# embeddings = local bge via langchain-huggingface.
ragas==0.2.15
langchain-groq==0.3.8
langchain-huggingface==0.3.1

# Utilities
tqdm==4.67.1
python-dotenv==1.1.1
groq==0.37.1
sec-edgar-downloader==5.1.0
